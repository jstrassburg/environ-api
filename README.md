A simple flask app to spit out the current environment variables.

This was used to test saving application configuration secrets in encrypted s3 buckets.

To run:

    pip install -r requirements.txt
    python app.py

